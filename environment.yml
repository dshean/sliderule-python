# conda env create -f environment.yml
name: sliderule
channels:
  - conda-forge
dependencies:
  - python=3.8
  - pip
  - requests
  - setuptools_scm
  - ipykernel
  - ipywidgets
  - matplotlib
  - numpy
  - scipy
  - pandas
  - geopandas
  - pyproj
  - shapely
  - cartopy
  - pip:
      - -e ./
